//This code the array function was used and array was printed

const values=[1,2];
values.push(3);//add the value to the array
console.log(values);
values.pop();//delete the value from the array at the end
console.log(values);
values.shift();//delete the  value from the array in the beginning
console.log(values);
values.unshift(10);//This will add the value value at beginning
console.log(values);
console.log(values.length);//return array length
console.log(values[0]);//accessing the array elements by the index